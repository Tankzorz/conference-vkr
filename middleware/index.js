export default function ({ store, redirect }) {
  // If the user is not authenticated
  //if (store.getters['login/loginStatus']) {
  //  return redirect('/')
  //} else if (!store.getters['login/loginStatus']) {
  //  return redirect('/Login')
  //}
}
